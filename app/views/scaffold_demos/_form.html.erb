<%= form_for(@scaffold_demo) do |f| %>
  <% if @scaffold_demo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scaffold_demo.errors.count, "error") %> prohibited this scaffold_demo from being saved:</h2>

      <ul>
      <% @scaffold_demo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
